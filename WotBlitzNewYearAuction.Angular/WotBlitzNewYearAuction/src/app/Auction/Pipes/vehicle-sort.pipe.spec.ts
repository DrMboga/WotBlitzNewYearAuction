import { VehicleSortPipe } from './vehicle-sort.pipe';

describe('VehicleSortPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleSortPipe();
    expect(pipe).toBeTruthy();
  });
});
