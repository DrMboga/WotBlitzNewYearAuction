import { NationBackgroundPipe } from './nation-background.pipe';

describe('NationBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new NationBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
