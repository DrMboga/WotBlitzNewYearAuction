import { Routes } from '@angular/router';
import { AuctionComponent } from './Auction/Components/auction/auction.component';

export const routes: Routes = [{ path: '', component: AuctionComponent }];
